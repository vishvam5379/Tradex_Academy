[project]
name = "tradex-academy"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "Django>=6.0",
    "pymysql>=1.2.0",
    "razorpay>=2.0.0",
    "python-dotenv>=1.0.0",
    "Pillow>=10.0.0",
    "whitenoise>=6.6.0",
]

[tool.vercel]
entrypoint = "academy_core.wsgi:application"
